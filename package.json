{
  "name": "Kotlin JS Sealed KSP Practice",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webApp",
    "shared/build/dist/js/developmentLibrary",
    "shared/build/generated/ksp/js/jsMain/resources/generated"
  ],
  "scripts": {
    "build": "cd webApp && npm run build",
    "start": "cd webApp && npm run start"
  }
}